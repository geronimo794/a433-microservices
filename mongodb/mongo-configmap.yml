apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-config-map
  namespace: karsa
  labels:
    app: mongodb
data:
  mongod.conf: |
    security:
      authorization: enabled
    net:
      bindIp: 0.0.0.0
