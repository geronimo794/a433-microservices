apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: karsa
  labels:
    app: vue
    tier: frontend
spec:
  type: NodePort
  ports:
  - port: 8000
    nodePort: 32000
  selector:
    app: vue
    tier: frontend
