apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: karsa
  labels:
    app: go
    tier: backend

spec:
  type: NodePort
  ports:
  - port: 8080
    nodePort: 31002
    protocol: TCP
  selector:
    app: go
    tier: backend
